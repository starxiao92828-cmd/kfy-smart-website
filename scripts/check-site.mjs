
import fs from 'node:fs';import path from 'node:path';
const root=process.cwd();const pages=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(['.git','node_modules'].includes(e.name))continue;const p=path.join(d,e.name);e.isDirectory()?walk(p):e.name.endsWith('.html')&&pages.push(p)}}walk(root);let errors=[];for(const p of pages){const html=fs.readFileSync(p,'utf8');for(const m of html.matchAll(/(?:src|href)="(\/[^"]+)"/g)){let u=m[1].split(/[?#]/)[0];if(u.startsWith('/api/')||u==='/'||u.startsWith('/mailto:'))continue;let target=path.join(root,u);if(u.endsWith('/'))target=path.join(target,'index.html');if(!fs.existsSync(target)&&!u.startsWith('/contact/?'))errors.push(`${path.relative(root,p)} -> ${u}`)}}if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`Checked ${pages.length} HTML pages: all local links/assets resolved.`);
